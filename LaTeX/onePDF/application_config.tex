% ============================================================
% APPLICATION PACKAGE PROJECT ADAPTER
% ============================================================
% Project-local aliases and derived values only.
% No user-editable facts, paths, language, visibility, order, or titles belong here.
% Target metadata is shared from ../src/content_cover_letter.tex.
% Language-dependent onePDF-owned naming is resolved here with \MyL.
% Application metadata itself is already generated in the selected language in ../src/.

\newcommand{\ApplicationPosition}{\TargetPositionTitle}
\newcommand{\ApplicationOrganisation}{\TargetOrganisation}
\newcommand{\ApplicationDepartment}{\TargetDepartment}
\newcommand{\ApplicationLocation}{\TargetCity}
\newcommand{\ApplicationReference}{\TargetReferenceNumber}
\newcommand{\ApplicationPDFTitle}{\MyL{Application}{Bewerbung} -- \ApplicantFullName -- \ApplicationPosition}
